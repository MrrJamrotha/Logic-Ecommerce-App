enum LanguageType {
  en,
  km,
}

enum ThemeType {
  light,
  dark,
}

enum BubbleType { sendBubble, receiverBubble }

enum MessageStatus {
  success,
  error,
  warning,
}

enum OrderColorStatus {
  pending,
  confirm,
  delivery,
  delivered,
}
